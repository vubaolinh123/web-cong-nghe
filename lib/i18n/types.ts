export type Language = 'vi' | 'en';

export type TranslationDictionary = {
  nav: {
    home: string;
    services: string;
    blog: string;
    about: string;
    contact: string;
    cta: string;
  };
  common: {
    language: string;
  };
};